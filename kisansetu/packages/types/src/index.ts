// Shared TypeScript types across apps/web and apps/api.
// Foundation phase: intentionally empty. Populate as domain types
// (User, ProduceListing, Order, etc.) are implemented (spec section 9),
// once the Prisma schema is the source of truth for these shapes.
export {};
