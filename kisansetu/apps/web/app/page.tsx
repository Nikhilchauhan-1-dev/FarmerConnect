// Foundation phase placeholder. Role-specific dashboards (farmer, buyer,
// transporter, admin) and auth flows are implemented in later tasks —
// see project spec sections 25 and 27–28.
export default function HomePage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>KisanSetu</h1>
      <p>Foundation build — application routes not implemented yet.</p>
    </main>
  );
}
